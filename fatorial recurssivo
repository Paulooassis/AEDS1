#include <stdio.h>

int fatorial(int num){
  if(num == 1){
    return 1;
  }
  if(num == 0){a
    return 0;
  }
  return num * fatorial(num-1);
}

int main(){
  int numero;

  printf("Digite qual termo quer o fatorial: ");
  scanf("%d", &numero);
  

  printf("O fatorial de %d e %d\n\n", numero, fatorial(numero));

}
